Customer-owned extensions live here.

Register extension definitions from this directory in `../overlay.config.ts`, and register local React component keys in the application's extension component registry.
