Overlay Web accepts browser sessions through the signed httpOnly `overlay_session` cookie. Native and service-style clients should use bearer access tokens only through routes that explicitly support them. Do not call WorkOS admin APIs, Stripe, R2, Convex, or model providers directly from client code.
